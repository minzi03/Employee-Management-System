# MongoDB connection (local)
MONGO_URI=mongodb://127.0.0.1:27017/scrum_master

# JSON Web Token secret (bắt buộc)
JWT_SECRET=super-secret-change-me

# Port server (backend)
PORT=4000

# Cho phép frontend truy cập (nếu server bật CORS)
CORS_ORIGIN=http://localhost:5173
